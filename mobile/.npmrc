legacy-peer-deps=true
# Flatten node_modules (npm-style) while staying on pnpm. Required on Windows:
# pnpm's default .pnpm/<pkg>@<ver>_<hash>/node_modules/ segment makes native
# C++ module paths exceed CMAKE_OBJECT_PATH_MAX, breaking the Ninja build.
node-linker=hoisted
