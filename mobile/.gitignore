# Dependencies
node_modules/
# package-lock.json is committed on purpose. `mobile` is outside the npm
# workspaces the root lockfile covers, so without its own lockfile two installs
# of the same commit can resolve different versions — and a native module that
# shifts under Expo is a broken build, not a warning (ENG-27).
yarn.lock
pnpm-lock.yaml

# Expo
.expo/
.expo-shared/
dist/

# Build outputs
build/
*.aar
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Play Store service account key (never commit)
google-play-service-account*.json
play-store-key*.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
logs/

# OS files
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.c9/
*.launch
.settings/
*.code-workspace

# Environment
.env.local
.env.*.local

# Testing
coverage/
.nyc_output/

# Android
android/.gradle
android/build
android/local.properties
modules/**/android/.gradle/
modules/**/android/build/
modules/**/android/.cxx/

# iOS
ios/Pods
ios/Podfile.lock
ios/build

# Misc
.cache/
.eslintcache
