node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
coverage/
.vscode/
.idea/

# Expo / RN
mobile/.expo/
mobile/android/
mobile/ios/
mobile/*.jks
mobile/*.keystore
mobile/google-services.json

# Vite
dashboard/dist/
dashboard/.vite/
*.tsbuildinfo

# Vercel CLI. `vercel pull` writes the project's real environment values into
# .vercel/.env.*.local, and `vercel build` writes the deployable output here.
.vercel/
.vercel/output/

# Local MongoDB data + logs (created by the single-node replica set used in dev)
.data/
# Added by code-review-graph
.code-review-graph/

# Local device captures and visual-design references
.design-references/
.vercel
